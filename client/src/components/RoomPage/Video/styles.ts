import { makeStyles } from '@material-ui/core/styles';

// Video styles
export const styles = makeStyles(() => ({
  container: {
    width: '100%',
    height: '100%',
    position: 'relative',
    // backgroundColor: 'blue',
  },
}));
